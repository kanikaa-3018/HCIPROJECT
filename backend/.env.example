PORT=5000
MONGODB_URI=mongodb://localhost:27017/mess-menu-system
JWT_SECRET=your_jwt_secret_key_change_in_production
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Optional: seed and allow only these admin account(s).
# Use ADMIN_EMAILS as a comma-separated allowlist if you have more than one admin.
ADMIN_EMAIL=admin@college.edu
ADMIN_PASSWORD=change_this_admin_password
ADMIN_NAME=Mess Admin
